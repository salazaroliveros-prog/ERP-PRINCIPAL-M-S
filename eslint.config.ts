// ESLint configuration - not used in production build
// This file is only used by IDE linting, not by Vite or TypeScript compilation
// If ESLint dependencies are not installed, this file can be safely ignored

// Uncomment when ESLint dependencies are installed (@eslint/js, globals, typescript-eslint, eslint-plugin-react)
// import js from "@eslint/js";
// import globals from "globals";
// import tseslint from "typescript-eslint";
// import pluginReact from "eslint-plugin-react";
// import { defineConfig } from "eslint/config";

// export default defineConfig([
//   { files: ["**/*.{js,mjs,cjs,ts,mts,cts,jsx,tsx}"], plugins: { js }, extends: ["js/recommended"], languageOptions: { globals: globals.browser } },
//   tseslint.configs.recommended,
//   pluginReact.configs.flat.recommended,
// ]);

export default [];
